{
  "name": "whatsapp-bot",
  "version": "1.0.0",
  "description": "Personal WhatsApp Bot with AI responses",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && npx puppeteer browsers install chrome",
    "postinstall": "npx puppeteer browsers install chrome || echo 'Puppeteer browser install failed, continuing...'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "ai",
    "gemini"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "whatsapp-web.js": "1.21.0",
    "express": "4.18.2",
    "qrcode": "1.5.3",
    "axios": "1.6.0",
    "cors": "2.8.5",
    "dotenv": "16.3.1",
    "puppeteer": "21.5.0"
  },
  "devDependencies": {
    "nodemon": "3.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
